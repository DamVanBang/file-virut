'use strict';

const path = require('path');
const lib = require('./lib');

// let project = lib.createProject(
// path.resolve(__dirname, '../../projects/it-comps'),
// path.resolve(__dirname, '../../packages')
// );
// project.fixChanges();

global.pk1 = lib.createPackage(
  path.resolve(__dirname, '../../projects/it-comps')
);

// global.p = project;
// global.pk = project.packages[1];
